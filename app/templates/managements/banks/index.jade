.page-header
  h1 Bank Manager

section#newsbank
  header
    h1 Add Bank
  form
    fieldset(data-role='form-group')
      label.sr-only(for="account-id") N° du compte
      input#account-id.form-control.new(type="text", 
        placeholder="Pour {#bank_name} ")
    fieldset(data-role='form-group')
      label.sr-only(for="account-password") Code Personnel
      input#account-password.form-control.new(type="password", 
        placeholder="Pour {#bank_name} ")
    fieldset(data-role='form-group')
      button.btn.btn-default(type="submit") Add bank

section#listbank    
  .bank-list